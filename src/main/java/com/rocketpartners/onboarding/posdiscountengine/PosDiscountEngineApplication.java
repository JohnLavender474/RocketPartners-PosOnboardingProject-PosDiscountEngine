package com.rocketpartners.onboarding.posdiscountengine;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main application class for the POS discount engine.
 */
@SpringBootApplication
public class PosDiscountEngineApplication {

    /**
     * The main method for the POS discount engine application.
     *
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(PosDiscountEngineApplication.class, args);
    }
}
